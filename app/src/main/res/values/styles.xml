<resources>

    <!-- Base application theme. -->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!--<item name="colorPrimaryDark">@color/colorPrimaryDark</item>-->
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@color/default_window_color</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowAnimationStyle">@style/animation_translucent_translate</item>
        <item name="android:windowDisablePreview">true</item>
    </style>

    <style name="AppTheme" parent="AppTheme.Base">
        <!-- 自定义 -->
    </style>

    <style name="Transparent" parent="AppTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <style name="AppTheme.FUllScreen" parent="AppTheme">
        <item name="android:windowFullscreen">true</item>
    </style>


    <style name="animation_translucent_translate" parent="@android:style/Animation">
        <!--<item name="android:windowEnterAnimation">@anim/slide_left_in</item>-->
        <!--<item name="android:windowExitAnimation">@anim/slide_right_out</item>-->
        <item name="android:activityOpenEnterAnimation">@anim/slide_left_in</item>
        <item name="android:activityOpenExitAnimation">@anim/slide_left_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/slide_right_in</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_right_out</item>
    </style>

    <style name="HomeBottomText">
        <item name="android:gravity">center</item>
        <item name="android:textColor">@drawable/selector_tab_text</item>
        <item name="android:drawablePadding">3dp</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="RateStyle" parent="@android:style/Widget.RatingBar">
        <!-- 定义星星图片 -->
        <item name="android:progressDrawable">@drawable/layer_rate_bar</item>
    </style>

    <style name="RateStyleMiddle" parent="@android:style/Widget.RatingBar">
        <!-- 定义星星图片 -->
        <item name="android:progressDrawable">@drawable/layer_rate_bar_middle</item>
    </style>

    <style name="RateStyleSmall" parent="@android:style/Widget.RatingBar">
        <!-- 定义星星图片 -->
        <item name="android:progressDrawable">@drawable/layer_rate_bar_small</item>
    </style>

    <style name="CommonCard" parent="Base.CardView">
        <item name="cardCornerRadius">6dp</item>
        <item name="cardElevation">6dp</item>
        <item name="cardMaxElevation">8dp</item>
        <item name="cardUseCompatPadding">false</item>
        <item name="cardPreventCornerOverlap">true</item>
    </style>
</resources>
